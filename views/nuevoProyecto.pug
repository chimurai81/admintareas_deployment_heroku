extends layout

include ./mixin/crearProyecto.pug

block contenido
    .contenido-principal
        h1 #{nombrePagina}

        //-pre= vardumps(proyecto)

        if errores
            each error in errores
                .alerta= error.texto
        +crearProyecto(proyecto)
        