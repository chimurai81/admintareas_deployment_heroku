<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body
        h2 Reestablecer Contraseña en AdminTareas
        p Hola, has solicitado reestablecer tu password, has click en el siguiente enlace para poder reetablecerlo, este enlace es temporal, en caso no te funcione, o vence, vuelve a solicitarlo.
        a(href=`${resetUrl}`) Ir a Cambiar Contraseña
        p si no puedes acceder a este enlace, visita #{resetUrl}
        p si no solicitaste tu el email, ignoralo.